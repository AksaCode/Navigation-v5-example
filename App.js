import React from "react";
// import of a Navigator that will be shown in the App
import Navigator from "./navigation/Navigator";

export default function App() {
  // use of Navigator
  return <Navigator />;
}
